# sources/B8/ -- Bond & DePaulo, *Accuracy of Deception Judgments* (2006)

Layer A dossiers for this source. One file per entry, named
`B8-T-nn-nn.tex`.

**APPEND-ONLY.** Write a dossier once and do not restructure
it later. If the source says something new about an entry,
add a bullet to that entry's dossier. Superseded material is
marked `@status: retired` with a `@reason:` -- never deleted.

Create dossiers with:

```
python3 tools/new_dossier.py --book B8 --topic T-01-01
```

Note: this source is a journal article mirrored as a text
transcript (`books/Bond & DePaulo 2006 - Accuracy of Deception
Judgments.txt`), not a PDF; `tools/extract_corpus.py` passes
`.txt` shelf files straight through to the corpus.
